{
  "name": "slack-emoji-forwarder",
  "version": "1.0.0",
  "private": true,
  "description": "Slack Events API app: forwards messages reacted with :skunk: into a thread in #coc-reports",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@slack/web-api": "^7.7.0",
    "@vercel/functions": "^1.5.0",
    "@vercel/kv": "^2.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
